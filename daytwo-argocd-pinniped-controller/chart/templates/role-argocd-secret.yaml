apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: role-argocd-secret
  namespace: {{ .Values.pinniped.argocdNamespace }}
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - update
  - patch
  - delete
  - watch
